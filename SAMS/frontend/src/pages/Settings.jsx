import {ShieldCheck,Database,Palette} from 'lucide-react';import {useAuth} from '../context/AuthContext';
export default function Settings(){const{user}=useAuth();return <div className="fade-in"><div className="resource-heading"><div><div className="resource-title"><div className="resource-icon">⚙</div><div><h1>Settings</h1></div></div></div></div><div className="settings-grid"><div className="setting-card card"><ShieldCheck/><h3>Role & access</h3><p>Current role: <b className="capitalize">{user?.role?.replace('_',' ')}</b></p><small>JWT authentication and role-based access are enforced by the backend.</small></div><div className="setting-card card"><Database/><h3>API connection</h3><p>{import.meta.env.VITE_API_URL||'http://localhost:5000/api'}</p><span className="status status-green">Configured via .env</span></div><div className="setting-card card"><Palette/><h3>Visual system</h3><p>Soft airport neutrals based on the supplied palette.</p><div className="palette-row">{['#B4B4B8','#C7C8CC','#E3E1D9','#F2EFE5'].map(c=><i key={c} style={{background:c}} title={c}/>)}</div></div></div></div>}
